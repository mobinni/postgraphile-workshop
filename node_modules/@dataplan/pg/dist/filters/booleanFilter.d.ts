import type { ExecutableStep } from "grafast";
import { ModifierStep } from "grafast";
import type { SQL } from "pg-sql2";
import type { PgCodec, PgConditionLikeStep } from "../interfaces.js";
export declare class BooleanFilterStep extends ModifierStep<PgConditionLikeStep> {
    readonly expression: SQL;
    static $$export: {
        moduleName: string;
        exportName: string;
    };
    private conditions;
    private havingConditions;
    alias: SQL;
    constructor($classFilterPlan: PgConditionLikeStep, expression: SQL);
    placeholder($step: ExecutableStep, codec: PgCodec): SQL;
    where(condition: SQL): void;
    having(condition: SQL): void;
    apply(): void;
}
//# sourceMappingURL=booleanFilter.d.ts.map