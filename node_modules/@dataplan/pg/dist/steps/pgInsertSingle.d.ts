import type { GrafastResultsList, GrafastValuesList, SetterCapableStep, SetterStep } from "grafast";
import { ExecutableStep } from "grafast";
import type { SQL } from "pg-sql2";
import type { PgCodecAttribute } from "../codecs.js";
import type { PgResource } from "../index.js";
import type { GetPgResourceAttributes, GetPgResourceCodec, PgTypedExecutableStep } from "../interfaces.js";
import type { PgClassExpressionStep } from "./pgClassExpression.js";
export declare class PgInsertSingleStep<TResource extends PgResource<any, any, any, any, any> = PgResource> extends ExecutableStep<unknown[]> implements SetterCapableStep<{
    [key in keyof GetPgResourceAttributes<TResource> & string]: ExecutableStep;
}> {
    static $$export: {
        moduleName: string;
        exportName: string;
    };
    isSyncAndSafe: boolean;
    hasSideEffects: boolean;
    readonly resource: TResource;
    private readonly name;
    private readonly symbol;
    readonly alias: SQL;
    private attributes;
    private contextId;
    private locked;
    private finalizeResults;
    private selects;
    constructor(resource: TResource, attributes?: {
        [key in keyof GetPgResourceAttributes<TResource>]?: PgTypedExecutableStep<GetPgResourceAttributes<TResource>[key]["codec"]> | ExecutableStep;
    });
    toStringMeta(): string | null;
    set<TKey extends keyof GetPgResourceAttributes<TResource>>(name: TKey, value: ExecutableStep): void;
    setPlan(): SetterStep<{
        [key in keyof GetPgResourceAttributes<TResource> & string]: ExecutableStep;
    }, this>;
    get<TAttr extends keyof GetPgResourceAttributes<TResource>>(attr: TAttr): PgClassExpressionStep<GetPgResourceAttributes<TResource>[TAttr] extends PgCodecAttribute<infer UCodec> ? UCodec : never, TResource>;
    record(): PgClassExpressionStep<GetPgResourceCodec<TResource>, TResource>;
    selectAndReturnIndex(fragment: SQL): number;
    execute(count: number, values: Array<GrafastValuesList<any>>): Promise<GrafastResultsList<any>>;
    finalize(): void;
}
export declare function pgInsertSingle<TResource extends PgResource<any, any, any, any, any>>(resource: TResource, attributes?: {
    [key in keyof GetPgResourceAttributes<TResource>]?: PgTypedExecutableStep<GetPgResourceAttributes<TResource>[key]["codec"]> | ExecutableStep;
}): PgInsertSingleStep<TResource>;
//# sourceMappingURL=pgInsertSingle.d.ts.map